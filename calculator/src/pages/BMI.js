import React from 'react';
import { Box, Heading, Text } from '@chakra-ui/react';

const BMI = () => (
  <Box p={8}>
    <Heading mb={4}>BMI Calculator</Heading>
    <Text>This is the BMI Calculator page. The calculator component will be implemented here.</Text>
  </Box>
);

export default BMI;
