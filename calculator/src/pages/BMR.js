import React from 'react';
import { Box, Heading, Text } from '@chakra-ui/react';

const BMR = () => (
  <Box p={8}>
    <Heading mb={4}>BMR Calculator</Heading>
    <Text>This is the BMR Calculator page. The calculator component will be implemented here.</Text>
  </Box>
);

export default BMR;
